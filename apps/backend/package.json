{
  "name": "@ai-jupyter/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest --verbose",
    "test:watch": "pytest-watch",
    "lint": "flake8 . && black --check .",
    "format": "black .",
    "type-check": "mypy ."
  }
}